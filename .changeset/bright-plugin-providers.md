---
"@cosystem/core": minor
"@cosystem/router": minor
"@cosystem/storage": patch
---

Allow plugins to contribute non-module providers, have the router plugin provide `RouterToken`, and flush storage writes through plugin context disposal.
