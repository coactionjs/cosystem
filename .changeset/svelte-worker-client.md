---
"@cosystem/svelte": minor
---

Add Svelte store and rune helpers for worker-hosted modules with `setWorkerClient`, `workerModuleStore`, `workerSelectorStore`, `workerModuleRune`, and `workerSelectorRune`.
