---
"@cosystem/core": patch
---

Upgrade the runtime dependency to Coaction 3.1.0, consume its reactive tracker
from the public `coaction/adapter` entry point, and preserve dynamic lazy-module
state behind Coaction's fixed root schema.
